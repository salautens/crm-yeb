export default function ProdutoList() {
  return (
    <div style={{ background: 'var(--color-bg-white)', border: '1px solid var(--color-border)', borderRadius: 12, padding: 32, textAlign: 'center', color: 'var(--color-text-muted)' }}>
      ProdutoList — em implementação
    </div>
  )
}
